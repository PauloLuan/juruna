import styled, { css } from 'styled-components'

import { {{properCase name}}Props } from '.'

export const Container = styled.div<{{properCase name}}Props>`
  ${({ theme, color }) => css`
    height: 100px;
    width: 500px;
  `}
`

export const Wrapper = styled.div<{{properCase name}}Props>`
  ${({ theme }) => css`
    background-color: red;
  `}
`
export const Main = styled.h1`
  color: black;
`
