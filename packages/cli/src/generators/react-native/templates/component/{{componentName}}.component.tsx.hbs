import * as S from './{{kebabCase componentName}}.styles'

export interface {{pascalCase componentName}}Props {
  testId?: string
}

export const {{pascalCase componentName}} = ({ testId = 'component-{{kebabCase componentName}}-id', ...props }: {{pascalCase componentName}}Props) => {
  return (
    <>
      <S.Wrapper testID={testId} {...props}>
        Body...
      </S.Wrapper>
    </>
  )
}
