import React from 'react'
import {Button, Text, TextInput, View} from 'react-native'
import {render, waitFor} from '@testing-library/react-native'

import { {{pascalCase componentName}} } from '.'
// import theme from '../../styles/Theme'

describe('<{{pascalCase componentName}} />', () => {
  const testId = 'component-{{kebabCase componentName}}-id'

  it('should render the {{pascalCase componentName}} component', () => {
    const {getByTestId, queryByTestId, toJSON} = render(<{{pascalCase componentName}} testId={testId} />)
    await waitFor(() => expect(queryByTestId(testId)).toBeTruthy())
    expect(toJSON()).toMatchSnapshot()
  })
})
